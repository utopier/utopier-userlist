import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// React 엘리먼트(<App/>)를 Container DOM(document.getElementById('root'))에 렌더링
ReactDOM.render(<App/>, document.getElementById('root'));

// react-scripts-ts